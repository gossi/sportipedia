info:
  name: Archive Apparatus
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{apiUrl}}/catalog/equipment/apparatuses/:id/archive-apparatus"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
