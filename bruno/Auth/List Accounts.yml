info:
  name: List Accounts
  type: http
  seq: 8

http:
  method: GET
  url: "{{authUrl}}/list-accounts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
