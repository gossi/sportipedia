info:
  name: Get (current) Session
  type: http
  seq: 6

http:
  method: GET
  url: "{{authUrl}}/get-session"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
