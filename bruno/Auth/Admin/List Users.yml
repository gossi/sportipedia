info:
  name: List Users
  type: http
  seq: 1

http:
  method: GET
  url: "{{authUrl}}/admin/list-users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
