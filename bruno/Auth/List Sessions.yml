info:
  name: List Sessions
  type: http
  seq: 7

http:
  method: GET
  url: "{{authUrl}}/list-sessions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
