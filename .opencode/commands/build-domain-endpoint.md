---
name: build-domain-endpoint
description: Implement an endpoint for a read or write CQRS/ES operation in the Sportipedia domain.
agent: domain-driven-developer
model: opencode-go/qwen3.6-plus
---

# Build Endpoint for Domain

Implement endpoint for operation: $1

1. /plan-domain-endpoint $1
2. Implement the plan with /tdd and /seek-implementation-for-endpoint

Now validate your own process in read-only mode:

- Where did you get information from required to implement?
- Was this the correct place to use these information?
- What needs to change to have the code compliant with the source of
  information?
- If no changes are needed, then this is good.
- If changes are needed, make suggestions how to tune the instructions to do so.
  Start a discussion.
